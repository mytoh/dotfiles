

set sheller=$HOME/.sheller
set plugins=(gauche panna pikkukivi fasd lehti nopea lista napa nyan)
set sheller_theme='kehote'
source $sheller/tcsh/sheller.tcsh




foreach f ($HOME/.complete.tcsh $HOME/perl5/perlbrew/etc/cshrc)
  if (-f $f) then
    source $f
    endif
end

