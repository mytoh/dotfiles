

set plugins=(gauche panna pikkukivi fasd lehti nopea lista)
set TCSH_THEME='kehote'
source ~/.tcsh.d/oh-my-tcsh.tcsh



foreach f ($HOME/.complete.tcsh $HOME/perl5/perlbrew/etc/cshrc)
  if (-f $f) then
    source $f
    endif
end

